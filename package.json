{
    "name": "ecosystem-simulator-root",
    "version": "1.0.0",
    "description": "Scientific Ecosystem Simulator Unified Project",
    "scripts": {
        "install:all": "npm install && cd backend-js && npm install && cd ../frontend && npm install",
        "dev": "concurrently \"cd backend-js && node server.js\" \"cd frontend && npm run dev\"",
        "dev:haskell": "concurrently \"cd backend && stack run\" \"cd frontend && npm run dev\"",
        "build": "npm run install:all && cd frontend && npm run build",
        "start": "cd backend-js && node server.js"
    },
    "devDependencies": {
        "concurrently": "^9.1.2"
    },
    "dependencies": {
        "cors": "^2.8.6",
        "express": "^5.2.1"
    }
}
